<Window x:Class="SecureFileExplorer.Client.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:SecureFileExplorer.Client"
        Title="Secure File Explorer - 技術部ファイルビューアー" Height="640" Width="1000"
        WindowStartupLocation="CenterScreen">

    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 上部: 検索欄・更新ボタン -->
        <Grid Grid.Row="0" Margin="0,0,0,6">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox Grid.Column="0" VerticalContentAlignment="Center" Padding="4"
                     Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                     ToolTip="ファイル名で検索">
                <TextBox.InputBindings>
                    <KeyBinding Key="Return" Command="{Binding SearchCommand}"/>
                </TextBox.InputBindings>
            </TextBox>
            <Button Grid.Column="1" Content="検索" Padding="12,4" Margin="6,0,0,0"
                    Command="{Binding SearchCommand}"/>
            <Button Grid.Column="2" Content="更新" Padding="12,4" Margin="6,0,0,0"
                    Command="{Binding RefreshCommand}"/>
        </Grid>

        <!-- パンくずリスト -->
        <ItemsControl Grid.Row="1" ItemsSource="{Binding Breadcrumbs}" Margin="2,0,0,6">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel Orientation="Horizontal"/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{Binding Name}" Foreground="#444"/>
                        <TextBlock Text="  &#x203A;  " Foreground="#999"/>
                    </StackPanel>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

        <!-- メイン: 左ツリー / 右ファイル一覧 -->
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="280" MinWidth="180"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- フォルダーツリー -->
            <TreeView x:Name="FolderTree" Grid.Column="0"
                      ItemsSource="{Binding RootFolders}"
                      SelectedItemChanged="FolderTree_SelectedItemChanged">
                <TreeView.ItemContainerStyle>
                    <Style TargetType="TreeViewItem">
                        <Setter Property="IsExpanded" Value="{Binding IsExpanded, Mode=TwoWay}"/>
                        <Setter Property="IsSelected" Value="{Binding IsSelected, Mode=TwoWay}"/>
                    </Style>
                </TreeView.ItemContainerStyle>
                <TreeView.ItemTemplate>
                    <HierarchicalDataTemplate ItemsSource="{Binding Children}">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="&#x1F4C1; " />
                            <TextBlock Text="{Binding Name}"/>
                        </StackPanel>
                    </HierarchicalDataTemplate>
                </TreeView.ItemTemplate>
            </TreeView>

            <GridSplitter Grid.Column="1" Width="4" HorizontalAlignment="Center"
                          VerticalAlignment="Stretch" Background="#EEE"/>

            <!-- ファイル一覧: 複数選択禁止 / D&D禁止 / コピー禁止 -->
            <ListView x:Name="FileList" Grid.Column="2"
                      ItemsSource="{Binding Files}"
                      SelectedItem="{Binding SelectedFile, Mode=TwoWay}"
                      SelectionMode="Single"
                      AllowDrop="False"
                      MouseDoubleClick="FileList_MouseDoubleClick"
                      PreviewKeyDown="FileList_PreviewKeyDown"
                      PreviewMouseMove="FileList_PreviewMouseMove">
                <ListView.InputBindings>
                    <KeyBinding Key="Return" Command="{Binding OpenFileCommand}"/>
                </ListView.InputBindings>
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="名前" Width="380" DisplayMemberBinding="{Binding Name}"/>
                        <GridViewColumn Header="種類" Width="80" DisplayMemberBinding="{Binding TypeLabel}"/>
                        <GridViewColumn Header="サイズ" Width="100" DisplayMemberBinding="{Binding SizeLabel}"/>
                        <GridViewColumn Header="更新日時" Width="140" DisplayMemberBinding="{Binding ModifiedLabel}"/>
                    </GridView>
                </ListView.View>
            </ListView>
        </Grid>

        <!-- 下部: 選択中ファイル情報 -->
        <Border Grid.Row="3" Background="#F5F5F5" Padding="6,4" Margin="0,6,0,0" CornerRadius="3">
            <TextBlock Text="{Binding SelectedFileInfo}" TextTrimming="CharacterEllipsis"/>
        </Border>

        <!-- ステータスバー -->
        <StatusBar Grid.Row="4" Margin="0,4,0,0">
            <StatusBarItem>
                <TextBlock Text="{Binding StatusText}"/>
            </StatusBarItem>
            <StatusBarItem HorizontalAlignment="Right">
                <ProgressBar Width="120" Height="12" IsIndeterminate="{Binding IsBusy}"
                             Visibility="{Binding IsBusy, Converter={StaticResource BoolToVis}}"/>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Window>
